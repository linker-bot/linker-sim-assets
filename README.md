# linker-robot-assets

Placeholder. Phase 2 of the open-source refactor populates this package with:
- the existing `assets/` tree (URDF / MJCF / XRDF + meshes)
- the composer + validators (currently `linker_sim.tools.composer`)
- the loader API (`asset_root()`, `load_manifest()`, `workstations()`)
- the hand decoders

See `docs/REFACTOR_PLAN.md` Phase 2 for the migration plan.
